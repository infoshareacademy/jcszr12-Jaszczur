@model GetStudentsAdRequestsResponse
@{
    ViewData["Title"] = "AdRequests";
}
    
<h1 class="title">Oczekujące zgłoszenia</h1>
<div class="ad-grid">
@foreach(var adRequest in Model.AdRequests)
{
    <partial name="@PartialNames.AdRequest" model="adRequest" />
    }
</div>
